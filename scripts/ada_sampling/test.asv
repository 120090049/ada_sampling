length = 45;
width = 21;
coordinates = generate_coordinates(length, width);
coordinates

function Xss = generate_coordinates(length, width)
    x = repmat(0:length-1, width, 1);
    y = repmat(0:width-1, 1, length);
    x = x(:)';
    y = y(:)';
    Xss = [x; y]';
    ksx_g = repmat(0:width-1, length, 1);
end
